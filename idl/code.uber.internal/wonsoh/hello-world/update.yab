# Learn more about yab templates by running: man yab
service: hello-world
method: HelloWorld::update
peer: http://localhost:6708/
thrift: hello_world.thrift
request:
  request:
    entity:
      entityID: ${id}
      name: ${name}
      email: ${email}
      phone: ${phone}